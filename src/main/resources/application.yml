spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user-order-service
    username: root
    password: utzpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  port: 8000

services:
  commodity-service:
    url: http://127.0.0.1