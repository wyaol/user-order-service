spring:
  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:test
    username:
    password:
  jpa:
    show-sql: true
  sql:
    init:
      schema-locations: classpath:db/schema.sql